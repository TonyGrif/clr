[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "clr"
version = "0.1.0"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
